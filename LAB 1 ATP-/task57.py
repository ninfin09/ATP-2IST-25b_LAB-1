for line in open(0):                   # Читаємо кожне значення маси пирога
    t = float(line)                    # Перетворюємо на число
    print(round(t * 0.4 * 0.7 * 1000, 1))  # Рахуємо і виводимо з округленням